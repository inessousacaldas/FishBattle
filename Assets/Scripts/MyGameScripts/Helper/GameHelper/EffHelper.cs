using AppDto;

public static class EffHelper
{
    public static string GetDaoGuangEffectName(int pModelID, ModelHelper.AnimType pAnimationName)
    {
//        if (pModelID <= 0 || string.IsNullOrEmpty(pAnimationName))
            return string.Empty;
//        //刀光特效的格式
//        public const string EFFECT_DAOGUANG = "dg_eff_{0}_{1}";
//        return string.Format(EFFECT_DAOGUANG, pModelID, pAnimationName);
    }
}